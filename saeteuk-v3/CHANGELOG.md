# 변경 이력

## v3.1.0 — 2026-09-21

### 새로 생긴 것

- **모델을 직접 적는다.** `⚙️ 설정` 시트 아래에 **🤖 모델 목록** 표(모델명 · 수준 · 언제 쓰나 · 회사 · 연결 확인)가 생겼다.
  이 표가 입력 시트·취합 시트·마법사의 `[모델]` 드롭다운 선택지가 된다. 목록에 없는 이름도 칸에 바로 적을 수 있다.
- **모델별 연결 테스트.** 메뉴 `② AI 연결 · 모델 설정`이 탭 세 개(API 키 · 모델 · 도움말)로 바뀌었다.
  모델마다 `[시험]` 버튼이 있고, 결과(✓ 연결됨 · 1.8초 / ✗ 이유)가 표의 `[연결 확인]` 칸에 남는다.
  메뉴 `모델 연결 테스트`는 목록 전체를 한 번에 확인한다.
- **오류를 우리말로.** 모델명 없음 · 무료 등급 불가(`limit: 0`) · 호출 한도 · 키 오류 · 잔액 없음 · 서버 불안정을
  구분해서 알려 준다. 다시 해도 소용없는 오류(모델명·무료 등급·잔액)는 재시도하지 않는다.
- **내 키로 쓸 수 있는 모델 불러오기.** 저장된 키로 각 회사의 모델 목록을 받아, 글쓰기에 못 쓰는 모델
  (이미지·음성·임베딩)을 빼고 보여 준다. 누르면 목록에 추가하고 바로 시험한다.
- **도움말** — 어느 수준이면 충분한지(예시 포함), 최신 모델명을 어디서 보는지, 테스트 결과 읽는 법.
- **시작하기의 AI 단계에서 모델도 고른다.** 기본값이 채워져 있고, 회사를 바꾸면 그 회사의 기본 모델로 바뀐다.
  성공하면 활동용·합본용 모델을 그것으로 맞추고 표에 ✓ 를 남긴다.
- **실행 시간 보호.** 한 번에 많이 생성할 때 5분이 지나면 스스로 멈추고, 남은 행은 체크를 그대로 둔다
  (예전에는 6분 제한에 걸려 오류로 끊겼다).

### 바뀐 것

- **기본 모델을 제미나이로 통일** — 활동용·합본용 모두 `gemini-flash-latest`.
  (이전: 활동용 `gemini-2.5-flash`, 합본용 `gpt-5-mini` — OpenAI 키가 없으면 압축이 실패했다.)
  `-latest` 이름은 구글이 새 Flash를 낼 때마다 자동으로 바뀌어 배포한 사본의 모델명이 낡지 않는다.
- 회사별 "충분" 모델: Gemini `gemini-flash-latest` · OpenAI `gpt-5.6-luna` · Claude `claude-haiku-4-5`.
- `[추가 모델]` 설정 칸은 없어지고 모델 목록 표로 합쳐졌다.

### 올라올 때

- `① 처음 설치 / 구조 복구`를 한 번 누르면 된다. 손대지 않은 옛 기본값(`gemini-2.5-flash`, `gpt-5-mini`)은
  새 기본값으로 올라가고, 선생님이 직접 고른 모델은 그대로 둔다. `[추가 모델]`에 적었던 이름은 모델 목록으로 옮겨진다.

### 테스트

- `test/models.test.js` (62개) — 이름 정리·회사 판별·오류 해석, 그리고 가짜 인터넷 위에서
  시험 → 표 기록 → 저장 → 생성 → 시간 제한까지.

## v3.0.1 — 2026-09-21

### 고친 것

- **[처음 설치]를 여러 번 눌러야 시트가 하나씩 생기고, 학생명단은 끝내 생기지 않던 문제.**
  1행에 안내문을 가로로 병합한 뒤 왼쪽 열을 고정했는데, 구글 시트는 병합된 셀을 가르는
  열 고정을 허용하지 않아 기록종류 → 교과영역 → 활동목록 차례로 멈췄다. 활동목록은
  데이터가 쓰이기 전에 멈춰서 매번 처음부터 다시 시도하다 같은 자리에서 멈췄고,
  그 뒤 순서인 학생명단에는 끝내 도달하지 못했다.
  - 설정용 시트(기록종류·교과영역·활동목록)는 열 고정을 뺐다.
  - 열 고정이 필요한 입력 시트·취합 시트는 1행을 [고정 구역]과 [나머지]로 나눠 병합한다.
  - 병합 전에 그 행의 기존 병합을 먼저 푼다(너비가 바뀌는 취합 시트 재생성 대비).
- **설치가 한 단계에서 실패해도 나머지는 끝까지 만든다.** 실패한 단계는 마지막에 모아 알린다.
- 반쯤 설치된 파일도 [처음 설치] 한 번으로 복구된다.

### 테스트

- `test/install.test.js` — 구글 시트의 병합·고정·범위 규칙을 흉내 낸 가짜 위에서 설치 →
  명단 → 활동 생성 → 취합까지 실제 코드를 끝까지 돌린다. 이 버그를 먼저 재현한 뒤 고쳤다.

## v3.0.0 — 2026-09

프롬프트 구조 전면 개편. v2와 시트 구조가 호환되지 않습니다. [이관 가이드](docs/MIGRATION-v2-to-v3.md) 참고.

### 바뀐 것

- **사본 배포 방식** — 마스터 사본을 한 번 만들어 `/copy` 링크로 나눠 주면, 받는 사람은
  [사본 만들기] 한 번으로 코드까지 복사됩니다. 깃허브에서 내려받아 붙여넣을 필요가 없습니다.
- **🚀 시작하기** — 사본을 처음 열면 안내 탭이 뜨고, 팝업이 구조 설치 → AI 키 → 학생 명단 →
  첫 활동 → 시험 생성까지 단계별로 안내합니다. 중간에 닫아도 이어집니다.
  권한 승인 화면("확인하지 않은 앱") 넘는 법도 여기서 설명합니다.
- **설치용 합본 빌드** — `node tools/build.js` 로 모듈 15개를 `dist/Code.gs` 한 개로 합칩니다.
  마스터를 만들 때 붙여넣을 파일이 5개로 줄었습니다.

- **프롬프트 조립 엔진** — 활동마다 프롬프트 전체를 복붙해 관리하던 방식을 없앴습니다.
  기록종류 × 교과영역 × 공통규칙 × 활동정의에서 7개 슬롯으로 자동 조립합니다.
- **기록 종류 도입** — 과세특 / 개세특 / 자율 / 동아리 / 진로 / 행동특성 및 종합의견.
  항목마다 글자수·서술 관점·성취수준 사용 여부가 따로 관리됩니다.
- **교과영역 분리** — 수학 전용 문구를 공통 영역에서 빼냈습니다. 10개 교과 프리셋 제공, 추가 자유.
- **활동 만들기 AI 마법사** — 활동을 말로 설명하면 입력 항목·개요·예시를 제안하고 시트까지 만듭니다.
- **기재요령 검증** — 생성 직후 분량·기재 금지사항·어미를 자동 점검합니다.
- **예시 관리** — 예시 칸 늘리기, AI로 예시 더 만들기, 예시 자체 검증.
  예시 시트에 바이트·검증 열이 생겼고, 프롬프트에는 앞에서부터 최대 8개까지 들어갑니다.
  마법사에서 활동을 만들 때 예시를 1~3개 함께 생성할 수 있습니다.
- **기록종류별 최종취합** — 과세특과 동아리를 한 덩어리로 합치던 문제를 고쳤습니다.
- **2026학년도 글자수 반영** — 진로활동 700→500자, 행동특성 및 종합의견 500→300자.
- **Anthropic(Claude) 지원 추가.**
- **일괄 생성 메뉴** — 체크박스 트리거 없이도 동작합니다.
- **학생 이름 마스킹** 기본 켜짐. API 키는 `UserProperties`에 사용자별 저장.
- **프롬프트 미리보기** — 실제로 AI에게 갈 문장을 만들기 전에 확인할 수 있습니다.

### 남은 것

- 수업관찰 웹앱은 사본마다 직접 배포해야 합니다(구글 정책).
- 구글폼 연동은 첫 세 문항이 반·번호·이름 단답형이어야 합니다.

## v2 — 2026 상반기

- 활동별 시트 자동 생성, 구글폼 연동, 수업관찰 웹앱, 최종 합본 압축.
- 수학 교과 세특 기준.
